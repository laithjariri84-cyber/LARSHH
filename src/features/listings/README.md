# Listings Feature

## Responsibility

Market offers: pricing, listing lifecycle, agent assignment.

## Status

Data model exists in Prisma (`Listing`). UI and services live in `search/` and `properties/` until this module is migrated.
